import React from 'react';
import InputLabel from "@material-ui/core/InputLabel/InputLabel";

const VerbItemComponent = ({verb} ) => (
    <div>
        <InputLabel>TEST</InputLabel><InputLabel>TEST2</InputLabel>
    </div>
);

export default VerbItemComponent;
